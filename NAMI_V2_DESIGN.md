# 航海士ナミ v2 設計方針

既存機能を削除せず、LINE版チャピとして理解層と出力層を整理する。

## 互換性の絶対条件
- 通常会話、単体見積、複数見積、画像/PDF、記憶、リマインド、自己改善を維持する。
- 既存の単体見積計算を唯一の計算経路として再利用し、複数物件でも物件ごとに独立実行する。
- 元資料にない物件情報・金額を生成しない。不明値は要確認。
- 計算済みテキストを画像/PDFへそのまま描画し、出力形式ごとに再計算しない。

## 見積UI
- 物件名、入居日、項目、内訳、金額、割引、合計を一画面で把握できること。
- 当月前家賃の内訳は「日割り」。日数が判明する場合は「日割り N日分」。
- 割引は赤で分離し、内訳に「割引額」を表示する。半額/無料などの条件も明示する。
- 合計は割引反映後の計算結果と一致させる。
- LINE転送を前提に高解像度PNGを生成する。

## 自然言語
「この3件」「全部」「全て」「すべて」「15日入居」「仲介半額」「安くして」「画像にして」などを文脈として理解し、固定コマンドだけに依存しない構成へ段階的に移行する。
