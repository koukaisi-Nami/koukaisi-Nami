# 航海士ナミ 見積機能 固定仕様

この仕様は既存の見積書デザイン・項目・図面抽出を壊さず拡張するための受入基準。

> Production deploy trigger: canonical estimate runtime / 2026-09-17

## 基準デザイン
既存 `estimate_document.py` の「見積もり概算書」を標準とする。項目・金額・合計・Steer Ship株式会社表記を維持する。

## 内訳欄だけ強化
- 当月前家賃: `日割りN日分` を内訳に表示する。
- 仲介手数料等の割引: `通常XXX円 / 割引YYY円` を内訳に表示し、金額欄は割引後金額。
- 合計は割引後金額だけを加算する。

## 入力
- 文面
- 写真/画像 1枚〜複数枚
- PDF 1ファイル〜複数ファイル
- 上記の混在

## 出力
- 文章
- 見積画像(PNG)
- PDF
- 画像+PDF

## 複数物件
- 物件ごとに独立して抽出・計算する。物件間で費用を混ぜない。
- 文章は1物件1メッセージ、画像は1物件1画像、PDFは1物件1PDF。
- 文章/PNG/PDFは必ず同一 canonical estimate object から生成する。

## データ優先順位
1. 募集図面/ユーザー文面に明記された条件
2. 既存の会社ルール/既定値
3. それでも不明なら要確認

図面に記載された敷金・礼金・保証料・火災保険・鍵交換・24時間サポート・事務手数料等を勝手に未確認へ戻さない。

## 必須回帰ケース
1. 画像1枚 -> 文章
2. 画像1枚 -> PNG
3. 画像1枚 -> PDF
4. 画像1枚 -> PNG+PDF
5. 複数画像/複数物件 -> 各物件の文章
6. 複数画像/複数物件 -> 各物件のPNG
7. 複数画像/複数物件 -> 各物件のPDF
8. PDF入力 -> 文章/PNG/PDF
9. 文面入力 -> 文章/PNG/PDF
10. 15日入居 + 仲介半額 -> 日割り内訳・通常額・割引額・割引後金額・合計が全形式で一致

見積依頼を通常チャットAIが自由作文して代替してはいけない。見積意図は見積Toolへルーティングする。
